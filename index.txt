import React, { useState } from 'react';
import { Button } from '/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '/components/ui/card';

export default function HelloWorldApp() {
  const [clickCount, setClickCount] = useState(0);

  const handleClick = () => {
    setClickCount(prev => prev + 1);
  };

  return (
    <div className="min-h-screen bg-background flex items-center justify-center p-4">
      <Card className="w-full max-w-md">
        <CardHeader className="text-center">
          <CardTitle className="text-3xl font-bold text-foreground mb-2">
            Hello World! 👋
          </CardTitle>
          <CardDescription className="text-muted-foreground">
            Welcome to your React app built with Shadcn/UI
          </CardDescription>
        </CardHeader>
        <CardContent className="text-center space-y-4">
          <p className="text-foreground">
            This is a simple Hello World application demonstrating modern React development.
          </p>
          
          <div className="space-y-3">
            <Button 
              onClick={handleClick}
              className="w-full"
              variant="default"
            >
              Click me! 
            </Button>
            
            {clickCount > 0 && (
              <div className="bg-muted p-3 rounded-md">
                <p className="text-sm text-muted-foreground">
                  Button clicked: <span className="font-semibold text-primary">{clickCount}</span> time{clickCount !== 1 ? 's' : ''}
                </p>
              </div>
            )}
          </div>

          <div className="pt-4 border-t border-border">
            <p className="text-xs text-muted-foreground">
              Built with React, TypeScript & Tailwind CSS
            </p>
          </div>
        </CardContent>
      </Card>
    </div>
  );
}
